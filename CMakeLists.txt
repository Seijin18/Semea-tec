# Arquivo CMakeLists.txt raiz para o workspace Semea-tec.
#
# Este arquivo ajuda IDEs (como o VSCode com a extensão CMake Tools) a reconhecer a raiz do projeto.
cmake_minimum_required(VERSION 3.16)
project(semea-tec)